{
  "name": "labos-course",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "pretest": "node -e \"if (typeof URL.canParse !== 'function') { console.error('Node.js >= 18.17 is required for Playwright. Run: nvm use 18.20.2'); process.exit(1); }\"",
    "test": "playwright test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "json-server": "json-server db.json --host 127.0.0.1",
    "chatbot": "node chatbot/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "allure-playwright": "^3.9.0",
    "prettier": "^3.8.3"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "better-sqlite3": "^12.10.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "json-server": "^1.0.0-beta.15"
  }
}
